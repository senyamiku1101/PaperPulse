name: Weekly Paper Update

on:
  schedule:
    - cron: '0 2 * * 1'    # 每周一 UTC 02:00
  workflow_dispatch:         # 允许手动触发

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest tests/test_all.py -v

      - name: Run full update pipeline
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          OPENALEX_API_KEY: ${{ secrets.OPENALEX_API_KEY }}
        run: |
          echo "=== Step 1: Fetch papers ==="
          python -m scripts.main --fetch-only

          echo "=== Step 2: Filter papers ==="
          python -c "from scripts.filtering import run_filter_pipeline; run_filter_pipeline()"

          echo "=== Step 3: AI analysis ==="
          python -m scripts.main --analyze-only --no-filter

          echo "=== Step 4: Generate trends ==="
          python -m scripts.main --trends-only

          echo "=== Step 5: Discover groups ==="
          python -m scripts.main --groups-only

          echo "=== Step 6: Generate summary ==="
          python -m scripts.main --summary-only

          echo "=== Pipeline complete ==="

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "PaperPulse Bot"
          git add data/
          git diff-index --quiet HEAD || git commit -m "chore: weekly paper update $(date +%Y-%m-%d)"
          git push
